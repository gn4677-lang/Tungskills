# OpenClaw Runtime Patterns

Use this file for long-lived assistant runtime thinking.

Doc-confirmed and partially code-confirmed patterns:
- the gateway owns long-lived session state
- session routing depends on source and scope
- typing and presence are runtime-level features
- cron jobs and delegates support proactive behavior
- failover and retry are explicit runtime subsystems
- skills load from multiple locations with precedence rules
